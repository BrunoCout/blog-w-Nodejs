<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">NodeJS</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Página inicial <span class="sr-only">(current)</span></a>
      </li>
      {{#if eAdmin}}
       <li class="nav-item active">
        <a class="nav-link" href="/admin/categorias">Categorias <span class="sr-only">(current)</span></a>
      </li>
      {{else}}
      {{/if}}
      {{#if user}}
      <li class="nav-item active">
        <a class="nav-link" href="/usuarios/logout">Sair<span class="sr-only">(current)</span></a>
      </li>
      {{else}}
      <li class="nav-item active">
        <a class="nav-link" href="/usuarios/login">Entrar<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="/usuarios/registro">Criar conta<span class="sr-only">(current)</span></a>
      </li>
      {{/if}}
    </ul>
  </div>
</nav>